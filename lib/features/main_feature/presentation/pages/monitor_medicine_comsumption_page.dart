import 'package:flutter/material.dart';

class MonitorMedicineConsumptionPage extends StatelessWidget {
  const MonitorMedicineConsumptionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Monitor Medicine Consumption'),
      ),
      body: const Center(
        child: Text('Monitor Medicine Consumption Page'),
      ),
    );
  }
}
