import 'package:flutter/material.dart';

class MonitorWaterConsumtionPage extends StatelessWidget {
  const MonitorWaterConsumtionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Monitor Water Consumtion'),
      ),
      body: const Center(
        child: Text('Monitor Water Consumtion Page'),
      ),
    );
  }
}
